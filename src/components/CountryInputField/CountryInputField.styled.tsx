import styled from "styled-components";
import { InputField } from "../../shared/StyledComponents";

export const CountryInputFieldStyled = styled(InputField)``;

export const Wrapper = styled.div`
  position: relative;
`;
